# A sample Guardfile
# More info at https://github.com/guard/guard#readme

# obsoleted by compass
# guard 'sass', :input => 'stylesheets', :output => 'public/stylesheets'
# can't get this to work, never triggers anything, so running compass manually for the time being
# guard 'compass', :configuration_file => "config/compass.rb"
